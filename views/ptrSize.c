#include <stdio.h>

int main() {
    // Print the size of an integer pointer
    printf("Size of Int Pointer: %lld", sizeof(int*));

    // Print the size of a character pointer
    printf("\nSize of Char Pointer: %lld", sizeof(char*));

    // Return 0 to indicate successful execution
    return 0;
}
